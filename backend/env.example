# ============================================
# CONFIGURACIÓN DE PRODUCCIÓN
# ============================================
# Copia este archivo a .env y configura los valores
# NUNCA subas el archivo .env al repositorio

# ============================================
# SEGURIDAD
# ============================================
# Genera una clave secreta única:
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure-dev-key-change-in-production

# DEBUG debe ser False en producción
DEBUG=False

# ============================================
# HOSTS PERMITIDOS
# ============================================
# Lista de dominios/IPs separados por comas
# Ejemplo: ALLOWED_HOSTS=tu-dominio.com,www.tu-dominio.com,192.168.1.100
ALLOWED_HOSTS=localhost,127.0.0.1

# ============================================
# BASE DE DATOS
# ============================================
DB_ENGINE=django.db.backends.postgresql
DB_NAME=trayectoria_estudiantil
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# ============================================
# CORS (Cross-Origin Resource Sharing)
# ============================================
# IMPORTANTE: Configura con tu dominio real en producción
# Ejemplo: CORS_ALLOWED_ORIGINS=https://tu-dominio.com,https://www.tu-dominio.com
# Si necesitas permitir cualquier origen (NO RECOMENDADO, solo si es necesario):
# CORS_ALLOW_ALL_ORIGINS=True
CORS_ALLOWED_ORIGINS=http://localhost:4200,http://127.0.0.1:4200

# ============================================
# REDIS (Para Celery)
# ============================================
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# ============================================
# OPCIONAL: Configuración adicional
# ============================================
# LOG_LEVEL=info
# TIME_ZONE=America/Bogota

